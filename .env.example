# PocketBase server URL for cloud sync
VITE_POCKETBASE_URL=https://pocketbase-production-c005.up.railway.app

# Set to "true" to disable cloud sync entirely (local-only mode)
VITE_DISABLE_CLOUD_SYNC=false

# Set to "true" to prevent writes to PocketBase (read-only mode)
VITE_PB_READONLY=false